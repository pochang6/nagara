# 変更の記録

新しいものから順に並べています。各版の詳しい理由はタグの付いたコミットに書いてあります。

## v1.2.1

- **自動再生とログイン時に起動が灰色になり、押せなくなっていた**のを直しました。
  選んでも閉じない項目は `view` を持ち、`action` を持ちません。そのため
  `NSMenu` の自動判定が「行き先の無い項目」と見て無効にしていました
- **メニューを開いている間、ショートカットキーが効かなかった**のを直しました。
  グローバルホットキーはキーを横取りするのに、メニューのトラッキング中はアプリまで
  届きません。開いている間だけ自前でキーを拾うようにしました
- 開いたまま速度や音量を変えたとき、先頭の状態行も一緒に描き直すようにしました

## v1.2.0

- **チェックの付く項目は、選んでもメニューを閉じなくなりました。**
  自動再生・速度・音量・声・エンジンの方針と待ち時間・ログイン項目が対象です。
  チェックを入れた瞬間に閉じられると、入ったことを確かめる場所がありません。
  閉じるのはメニューの外を押したときと esc のときだけ。「停止」「ログを開く」の
  ような1回きりの項目は今までどおり閉じます
- README に「読み間違いに気づいたら」を追加しました。読みの管理はエンジンの仕事なので、
  nagara には辞書機能を置かず、AI エージェントに頼んで AivisSpeech の
  ユーザー辞書へ入れてもらう道を書いています

## v1.1.0

- **出力先が変わっても鳴り続けるようになりました。**
  AirPods をケースに戻した拍子に既定の出力デバイスが差し替わると、音だけが消えて
  そのまま無音になっていました。デバイスの入れ替わりを見張り、鳴っている時間が
  止まっていたときだけ作り直します。うまく引き継げているときは何もしないので、
  途切れない乗り換えはそのままです
- **エンジンを閉じるまでの時間を選べるようになりました。**
  15分・30分・45分・1時間・2時間・6時間。段は `engineIdleQuitLadder` で足し引きできます
- 「起動したままにする」を選ぶと、隣の項目が「0分使わなければ閉じる」と
  名乗っていたのを直しました

## v1.0.1

- **音量を追加しました。** ⌃⌥= / ⌃⌥- で1段ずつ、メニューからも直接選べます。既定は 80%
- エンジンの所有権を pid でファイルに残すようにしました。再ビルドや強制終了で
  nagara だけが入れ替わっても、居座ったエンジンが迷子にならなくなります
- 隠して起こしたエンジンの窓が開いたら、そこで所有権を手放します。
  読み上げのついでに AivisSpeech で作業を始めたのに、その最中に落とされるのを防ぎます

## v1.0.0

- 最初の公開版
