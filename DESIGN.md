# DESIGN

判断と、その理由。**何をしたか**より**なぜそうしなかったか**を残すための文書。
ここを読まずに手を入れると、同じ議論を最初からやり直すことになる。

---

## 1. スクリーンリーダーではなく、プレイヤー

読み上げアプリには全く違う2つの設計がある。

**A. スクリーンリーダー型** — 画面に出ている文字を拾って読む。macOS の VoiceOver がこれ。
メニューバーもボタンのラベルもウィンドウのタイトルも読む。

**B. 受け口型** — アプリ自身は画面を見ない。テキストを投げ込まれたら喋る、それだけ。

**nagara は B。A は作らない。**

この一点が決まると、ほとんどの不安が消える。「画面全体に作用するならメニューバーまで
読まれてうるさいのでは」という懸念は、B なら原理的に起こらない。読むのは明示的に渡された
テキストだけで、画面のスキャンも OCR もアクセシビリティツリーの巡回も一切しない。

リポジトリの説明文にある「**Player** アプリ」という語が、この設計を一語で表している。

## 2. 入口は3つ、本体はひとつ

本体（`nagara.app`）は1種類しかなく、テキストの出どころを知らない。知る必要がない。
入口は薄いスクリプトで、あとから何本でも足せる。

| 入口 | 実装 |
|---|---|
| Claude Code の Stop フック | `hooks/nagara-stop-hook.sh` |
| CLI | `bin/nagara` |
| 右クリック（選択テキスト） | Info.plist の `NSServices` |

nobetsu との対称性で覚えるとよい。

- **nobetsu** = フォーカスのある場所へ**入れる** → どのアプリでも効く
- **nagara** = 選択した場所から**取る** → どのアプリでも効き、余計なものは入らない

どちらも「アプリごとの対応」を持たないから、どこでも動く。

## 3. フックは常に投げる。ただし鳴らさない

**自動再生の既定は OFF。** これは手抜きではなく、この設計の中心にある。

- 全部自動で喋られるのはうざい
- かといって毎回カーソルで擦って選択するのも嫌
- 「これは聴きたい」と思ったものだけ、**事後に**再生したい

だから Stop フックは応答のたびに黙って本文を届け、**キューに積むだけ**にする。
すると「聴きたい」と思った時点で、それはもう手元にある。あとは引き金を引くだけになる。

引き金は3通りあるが、どれも同じ動作をする。

| 引き金 | トークン | レイテンシ |
|---|---|---|
| `⌃⌥P` | ゼロ | ゼロ |
| `/speak` | ほぼゼロ | ほぼゼロ |
| 「喋って」と話す | ツール呼び出し1回分 | 1往復 |

## 4. 読み上げ命令に本文を渡さない

`/speak` も、将来 MCP ツールを作る場合も、**引数にテキストを取らせてはいけない**。

`speak("長文全部…")` にすると、LLM に応答をもう一度丸ごと出力させることになる。
トークンが倍かかり、その分遅くなり、しかも要約されて元と違う文章になりかねない。
フックがすでに届けているものを指すだけにする。

### 付随する罠

「喋って」と言うと LLM は「再生しました」と短く返す。**その返事もフックがキューに入れてしまう。**
放置すると次の再生でそれを朗読する羽目になる。対処は2段構え。

- フック側: 直前のユーザー発言が `/speak` `/stop` などで始まっていたら積まない
- 本体側: `minimumLength`（既定12文字）より短いものは積まない

## 5. 速度は再生側で変える。`speedScale` は使わない

AivisSpeech の `audio_query` には `speedScale` があるが、**使わない**。理由は2つ。

1. 合成をやり直すことになるので、**再生中に速度を変えられない**
2. 値を上げると読み上げの抑揚まで崩れる

代わりに `AVAudioEngine` に `AVAudioUnitTimePitch` を挟み、`rate` を変える。
**声の高さを保ったまま**速度だけ変わり、再生中でも即座に切り替わる。

`AVAudioUnitVarispeed` ではないことに注意。あちらは声が高くなる。

## 6. シークは秒ではなく文で

動画プレイヤーを真似て「15秒戻し」にすると、文の途中に着地して外す。

**文ごとに合成してキューに積み、文の境界をそのままチャプターにする。**
すると「1文戻る」が押せる。読み上げにはこちらのほうが合っている。

副産物として、長文でも**最初の1文ができ次第で鳴り始める**。全文を先に合成すると
待たされるので、先読みは3文ぶんに留めている（`prefetchDepth`）。

将来テキストを並べてカラオケ式にハイライトする場合も、この文単位の進行がそのまま使える。
**第2段階（アバター）へ繋ぐ接続点もここ**で、「いま何文目を喋っているか」が外に出せる形に
なっていれば、口パクと表情は後から足せる。

## 7. ホットキーは Carbon の `RegisterEventHotKey`

**許可が一切要らないから。**

nobetsu が入力監視の許可を必要としたのは、⌘ 単体の長押しという「修飾キーだけの操作」を
見張るために `CGEventTap` が要ったから。修飾キーとの和音であれば Carbon の API で足り、
入力監視もアクセシビリティも要求されない。System Settings での許可作業がまるごと消える。

### 選択テキストの経路は2本用意する

macOS のサービス（`NSServices`）は素の AppKit のテキスト欄でしか出ない。**Electron 製の
アプリはコンテキストメニューを自前で描くので、サービスが載らない。** Claude Code の
デスクトップ版がまさにそれで、右クリックしても「nagara で読む」は現れない。

そこで `⌘C` → `⌃⌥C`（クリップボードを読む）を用意した。**どのアプリでも通り、許可も要らない。**
選択テキストを直接読む API（AX）を使えば `⌘C` は省けるが、アクセシビリティの許可が要る。
「許可が一切要らない」という性質のほうが価値が高いと判断した。

### メニューにショートカットを出す

忘れたときに確かめる場所が要るので、`NSMenuItem` のキー等価物を実際に設定して
AppKit の標準の見た目（右寄せ・薄字）で描かせている。

副作用として、メニューを開いている間は**メニュー側とグローバルホットキーの両方が反応する**。
2回動いて何も起きていないように見えるので、`Controller.menuIsOpen` が立っている間は
グローバル側を黙らせている。

### `⌥⌘→` / `⌥⌘←` を避けた理由

Chrome・Safari・VS Code が「次のタブ／前のタブ」に既定で割り当てている。
グローバルホットキーとして奪うと、**全アプリでタブ切り替えが壊れる**。
`⌃⌥Space` も「次の入力ソース」に当たっているので使えない。`⌃⌥` 系のそれ以外はほぼ空いている。

## 8. エンジンの生かし方

起動のたびに8秒待つのも、一度きりの用事のために一日中居座られるのも、どちらも嫌。
折り合いはこう。

- 必要になった時点で `open -g -j` で裏起動する
- **一度起きたら置いておく**（連続で使う日に8秒を繰り返さない）
- **15分使われなければ静かに落とす**（一度きりの日に居座らない）
- **手で閉じられたら追いかけない**（次に必要になったときだけ、また起こす）
- 落とすのは**自分で起こしたエンジンだけ**。手で開かれたものには触らない

これでオプションを増やさずに済む。メニューには3択だけ置いてある。

### 既知の限界

AivisSpeech は Electron 製で、起動時に自分でウィンドウを前面へ出す。`open -g -j` でも
抑えきれないので、起動後しばらくは見つけ次第 `hide()` し続けている。**一瞬ちらつくのは
避けられない。** 完全に消すには GUI を持たない AivisSpeech-Engine 単体版に差し替えるしかない。

## 9. Markdown の除去は最初から真面目にやる

既存の読み上げツールが軒並み弱いのがここ。長文を流し聞きすると
「バッククォート バッククォート バッククォート」を延々聞かされる。

`Sanitizer` は「読んで意味のあるものだけ残す」方針で、コードブロックは丸ごと落とし、
リンクは表示文字だけ残し、表はセルを「、」で繋ぎ、飾り記号は剥がす。

## 10. ライセンス

nagara は AivisSpeech を**同梱もリンクもせず、別プロセスに HTTP で話しかけるだけ**。
したがって LGPL-3.0 の継承は発生せず、nagara 自体は MIT で出せる。

**注意すべきはエンジンではなくモデルのほう。** AivisHub のモデルは ACML / ACML-NC / CC0 など
モデルごとに条件が違う。nagara はモデルを同梱しないので nagara のライセンスには影響しないが、
**デモ動画や記事に音声を載せるときは、そのモデルの規約が効いてくる。**

---

## この先

- **Now Playing 対応** — `MPNowPlayingInfoCenter` + `MPRemoteCommandCenter` に名乗り出れば、
  メディアキー・コントロールセンター・AirPods のつまみ・Bluetooth ヘッドホンのボタンが
  そのまま効く。`skipBackwardCommand` と `changePlaybackPositionCommand` も標準で来る
- **任意位置へのシーク** — 文の境界に加えて、文中の位置も持てば実現できる
- **Codex 連携** — ターン完了時に外部プログラムを呼ぶ設定があるはずだが、
  **渡ってくる中身に応答本文が含まれるかを未確認**。含まれなければ CLI 経由になる
- **アバター（第2段階）** — macOS ネイティブで VRM は茨。透明ウィンドウ + `WKWebView` +
  three.js + `@pixiv/three-vrm` が現実解。AITuberKit をフォークして「LLM 無効・外部テキスト
  注入モード」を足すのが最短。表情は AivisSpeech の style（コハク・まお・阿井田茂が持っている）
  と同じ感情ラベルで駆動する
